{
  "name": "@sextant/pi-bus",
  "version": "0.1.0",
  "description": "A pi extension that makes a pi coding-agent session a first-class sextant bus client (TASK-177): its own scoped identity, addressable, wakeable on inbound frames, with bus tools, a /set-goal command over @sextant/conv-goals, a bundled sextant skill, and a pi.activity bridge so the dash renders a headless worker like any crew member.",
  "type": "module",
  "exports": {
    ".": {
      "types": "./dist/src/index.d.ts",
      "import": "./dist/src/index.js"
    }
  },
  "files": [
    "dist/src",
    "skill",
    "FINDINGS.md",
    "README.md"
  ],
  "engines": {
    "node": ">=22"
  },
  "sextant": {
    "epoch": 1
  },
  "scripts": {
    "build:deps": "npm --prefix ../sdk install && npm --prefix ../sdk run build && npm --prefix ../conventions/goals install && npm --prefix ../conventions/goals run build",
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "test": "npm run build && node --test dist/test/backpressure.test.js dist/test/gate.test.js dist/test/activity.test.js dist/test/goal_command.test.js",
    "driven": "npm run build:deps && npm run build && node dist/test/driven.js"
  },
  "license": "MIT",
  "dependencies": {
    "@earendil-works/pi-coding-agent": "0.79.8",
    "@sextant/conv-goals": "file:../conventions/goals",
    "@sextant/sdk": "file:../sdk",
    "typebox": "1.1.38"
  },
  "devDependencies": {
    "@types/node": "^26.0.0",
    "typescript": "^6.0.3"
  }
}
