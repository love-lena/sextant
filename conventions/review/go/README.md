# review/go — reserved

The Go peer of the review convention. The TS side (`conventions/review/ts`) is the
existing implementation; this Go slot is **reserved, not yet built**.

Implementing it (the Go convention + review conformance vectors, co-equal with the
TS side) is **TASK-239**. The ADR-0049 restructure only relocates the existing
sides and reserves the peers, so it stays purely behaviour-preserving.
