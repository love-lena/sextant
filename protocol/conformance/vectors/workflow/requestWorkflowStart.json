{
  "epoch": 1,
  "convention": "workflow",
  "verb": "requestWorkflowStart",
  "description": "workflow: request the coordinator to start a run — a single message.publish of a workflow.start on msg.topic.workflow.start. The dash's nonce is echoed in the coordinator's ack; empty optionals (target/by) are omitted.",
  "input": {
    "prompt": "review and merge the PR",
    "nonce": "01NONCE",
    "nickname": "reviewer"
  },
  "operations": [
    {
      "op": "message.publish",
      "subject": "msg.topic.workflow.start",
      "payload": {
        "$type": "workflow.start",
        "prompt": "review and merge the PR",
        "nonce": "01NONCE",
        "nickname": "reviewer"
      }
    }
  ]
}
