{
  "name": "sextant-workspace",
  "version": "0.0.0",
  "private": true,
  "description": "npm workspace root — links @sextant/client (clients/typescript) into images/sidecar/entrypoint without a checked-in symlink. Host workflows (make lint-sidecar / test-sidecar) run `npm install` from here. The published sidecar container image uses its own file: dep wiring inside the Dockerfile and does not need this workspace root.",
  "workspaces": [
    "clients/typescript",
    "images/sidecar/entrypoint"
  ]
}
