# OS
.DS_Store
Thumbs.db

# Editors
.idea/
.vscode/
*.swp
*.swo
*~

# Go
*.test
*.out
/bin/
/dist/

# Node / TypeScript
node_modules/
*.tsbuildinfo

# mdbook build output (the rendered site; src/ is canon)
/docs/book/book/

# Ephemeral plane (ADR-0002): the agent workspace — specs, work-plans,
# breakdowns. Never committed; committed ⇔ signed-off.
/.work/

# Installed skill files, materialized from skills-lock.json by the
# mattpocock-skills installer. The lock is the committed manifest; the
# fetched skill bodies are install artifacts.
/.claude/skills/
/.agents/
/.superpowers/
